name: WeatherMCP
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/WeatherMCP
  original_url: https://huggingface.co/spaces/Chris4K/WeatherMCP

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
